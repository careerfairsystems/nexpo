coverage:
  status:
    project:
      default:
        target: auto  # never decrease project wide coverage

ignore:
  - "test/support/*"

comment:
  layout: "header, diff"
  behavior: default # post once then update, posts new if delete

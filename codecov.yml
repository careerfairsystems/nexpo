coverage:
  status:
    project:
      default:
        target: auto  # never decrease project wide coverage

    patch:
      default:
        target: 90%   # make sure atleast 90% of new code is covered

ignore:
  - "test/support/*"

comment:
  layout: "header, diff"
  behavior: default # post once then update, posts new if delete
